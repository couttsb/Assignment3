package ca.mcgill.ecse321.loyaltypoints;

public class OperationPerDollarPoints implements PointsStrategy{
	@Override
	public double getPoints(double price) {
		return (double) Math.floor(price);
	}
}